This is a work-in-progress frontend for trunk-transcribe, built with Next.js and React InstantSearch.

## Start the project

```sh
npm install
npm run dev
```

This assumes using the default values from `.env.example`.
